# v1-4-upgrade：开发报告

- 报告 ID：`RPT-V1-4-UPGRADE-20260816T050313Z-001`
- 记录时间（UTC）：`2026-08-16T05:03:13Z`
- 报告类型：`development`
- 作者角色：`development`
- 独立性：`non_independent`
- 产品版本：`v1.4.0`
- 裁定范围：`archive_local`
- 裁定：`accepted`

## 范围

v1.4.0 升级周期的开发自测记录，覆盖四项新需求与一组正确性修复。关联需求：`REQ-050`、`REQ-051`、`REQ-052`、`REQ-053`。本报告仅为开发角色自述，不构成独立测试或验收结论。

### REQ-050 分类双字段

- 分类模型升级为「领域 domains 多选 × 体裁 genres 单选」双字段；`GET /taxonomy` 作为唯一下发源，前端分类界面同源消费。
- SQLite schema v9 迁移落库；可移植归档（备份/导出）schema v8 承载双字段往返。

### REQ-051 可配置媒体 AI

- 转写与理解双组独立配置（各自 base URL、模型、凭据引用）。
- 两层级联理解链路：转写 → 完整性判定 → tier1/tier2 摘要。
- 依赖治理：`litellm==1.96.2` 为本周期唯一新增锁定依赖（REQ-046 锁定治理）。

### REQ-052 AI 出站校验与凭据隔离

- `validate_ai_base_url` 出站地址校验，拒绝不合规端点。
- AI 凭据集中存放于数据根下 `state/ai/credentials.json` 凭据文件，与配置分离。
- AI 链路错误信息脱敏，不回显凭据、请求体与远端原始报错正文。

### REQ-053 场景感知关键帧采样

- 场景检测与等距采样混合策略，5%/95% 锚定，黑帧拒绝。
- 帧级 reason 持久化，随可移植归档 schema v8 往返。

### 同周期修复与功能化

- 视频分析正确性修复：分析前 verify 前置、帧尺寸修正、多 analysis 结果可见性。
- 检索修复：ffmpeg-local 退出全文检索、分类标签退出 haystack、domain/genre/_none/topic_id 过滤。
- 主题/关系功能化：管理端点、导航、主题版本链、same-work 候选。
- 图片分析独立 `image_*` 设置，与视频 AI 配置解耦。
- 归档验证器口径修正（本阶段发现）：登记一致性检查此前强制所有已登记验收报告为 `independent`，与政策「non_independent 验收的候选可被接受登记、但不得进入推荐位」冲突，导致工具化批次登记后任何新构建都被拒绝；已将独立性强制收敛到推荐快照检查（`_validate_version_summary_chain`），并新增回归测试（non_independent 可登记、不可推荐）。

### 缺陷闭环

- 缺陷台账 60 条；本周期 16 条全部 `resolved_locally`：DEF-SEARCH-002、DEF-VID-001..008、DEF-TAX-001、DEF-SEARCH-003、DEF-SEARCH-004、DEF-TAX-002、DEF-IMG-001、DEF-TOPIC-001、DEF-REL-001。

## 验证

- 单元套件（最终全量）：347 passed / 2 skipped（全量约 25 分钟）。此前一轮出现 1 failed，定位为台账计数断言随本周期条目增长失配，修正台账计数后复跑 `tests/unit/test_v1_archive.py` 36 passed 确认；验证器口径修正及新增回归测试后，该套件为 37 passed / 1 warning（用例预期告警）。
- 集成测试：`tests/integration/test_local_full_chain.py` 3 passed（含 AI 全链路）。
- 前端构建：tsc 类型检查与 vite 生产构建通过。
- 归档预检：`scripts/archive_v1.py --check-tree` 通过，输出 `{"baseline_files": 211, "defects": 60, "evidence_entries": 4, "reports": 57, "status": "passed"}`。

## 结论

- 本周期开发内容在本地范围完成并自测通过；本报告为 `non_independent` 开发自述。
- 双件制纪律：本报告与同名 JSON 侧车成对收录，JSON 为机器验证事实来源；后续更正一律追加新报告并以 `supersedes_report_id` 指向本报告，不改写本件。
- 本报告不构成发布批准：`release_readiness` 保持 `blocked`（REQ-045 真实 PostgreSQL 迁移/还原与 Compose 拓扑、REQ-001/REQ-044 浏览器黑盒门禁不在本周期范围）。
