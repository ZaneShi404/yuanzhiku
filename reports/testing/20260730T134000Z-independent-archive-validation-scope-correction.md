# 20260730 独立归档契约验证范围更正

## 初始验证结果

独立测试角色在不编辑工作树、不安装依赖、不修改网络或系统配置的条件下，对冻结版本完成了以下隔离验证：

- 归档构建器、独立验证器和归档单元测试的编译检查通过。
- 归档契约回归：`14 passed, 0 failed, 0 skipped, 1 warning`，耗时 `18.71s`。
- 完整单元套件：`110 passed, 0 failed, 2 skipped, 2 warnings`，耗时 `459.74s`。
- 两项完整套件 warning 均为负向安全测试刻意写入重复 ZIP 成员时由标准库发出的 warning，涉及 `records.json` 与 `manifest.json`；不代表候选档案包含重复成员。

运行使用新的隔离测试根、项目虚拟环境、关闭字节码写入和 pytest cache。未访问或修改日常 `data/`，也未在项目 `archives/` 下构建档案。

## 范围更正

测试角色随后确认：初始化定位命令的排除模式未按预期生效，曾枚举两个既有 V1 封存目录中同名文件的路径。未读取这些文件内容，未打开既有目录或 ZIP，未修改任何封存产物。

这不改变已完成测试的实际通过结果，但不满足本次独立测试任务“不得读取、修改或枚举既有封存档案”的严格范围。该运行因此只保留为过程记录，不作为下一后继档案的最终独立验证依据。

## 后继处置

在工作树继续保持冻结的条件下，由新的独立测试角色以精确命令重新执行编译、归档契约回归和完整单元套件；禁止任何项目扫描、档案路径发现、读取或枚举既有 `archives/` 内容。只有该重运行的实际结果才可写入下一候选档案的本地验证记录。
