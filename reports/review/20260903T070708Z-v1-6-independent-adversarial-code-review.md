# v1-6-independent-adversarial-code-review：复核报告

- 报告 ID：`RPT-V1-6-INDEPENDENT-ADVERSARIAL-CODE-REVIEW-20260903T070708Z-001`
- 记录时间（UTC）：`2026-09-03T07:07:08Z`
- 报告类型：`review`
- 作者角色：`review`
- 独立性：`non_independent`（复核由全新上下文的自动化代理执行——未经历实现过程、不继承实现假设，但与实现者同属一个编排会话，按政策口径如实标注）
- 产品版本：`v1.6.0`
- 裁定范围：`archive_local`
- 裁定：`accepted`

## 范围

对 v1.6.0 加固周期实现分支（当时 19 个提交，`0ab8f3e…d04d87f` 区间；本报告引用的提交哈希均早于发布前历史重写，对照见映射文件）的**对抗性代码复核**：默认"实现可能有问题"，逐项核对 P1 安全修复的 diff 本体、亲自重跑全部验证、审查计划偏差与剩余风险记录的诚实性。关联需求：`REQ-003`（日志与错误脱敏）、`REQ-040..042`（备份/导出/完整性）、`REQ-046`（依赖纪律）、`REQ-052`（出站校验）。

复核依据：加固实施计划（16 任务，含经批准偏差：Task 3 砍除、Task 4 拆分仅 4a、Task 8 代表性覆盖、Task 10 两子项未做）与四份现行规范。

## 验证

| 检查 | 结果 |
|---|---|
| 提交结构：恰 19 提交、线性、无 master 污染；diff 59 文件无凭据/Cookie/日常数据内容 | pass |
| P1 对账：凭据 ACL 断继承仅授三主体；损坏凭据 503 且原文件字节不变；Host/Origin 边界全变体拒绝（大小写/尾点/多冒号/`localhost.evil.com`）；httpx `trust_env=False` 全覆盖；上传主机发送前校验且失败零出站；relay 恒时比较 + 固定公网地址 | pass |
| 一致性对账：`commit_job_success` 三重栅栏（state/token/租约有效）且业务写入同事务；除幂等 sweeper 外全部成功路径走栅栏提交（逐一核对 10 处提交点）；确定性 ID 身份冲突显式抛错不折叠；删除屏障 BEGIN IMMEDIATE/FOR UPDATE；清理任务先于 catalog 删除入队；恢复 manifest 锚定 + 单句柄 + 暂存发布；再导入全量 artifact 修复 | pass |
| 亲自重跑：后端全量 489 通过、4 跳过、0 失败（约 31 分钟，退出码 0）；前端 test 9/9 + 检查 + 构建 exit 0；OpenAPI 68 操作零删除/零改名 | pass |
| 边界实证 12 探针：恶意 Host 全变体 403；恶意 Origin 与 `Origin: null` 写请求 403；GET 与无 Origin 按契约放行 | pass |
| 数据演练证据：只读快照计数一致、凭据与 Cookie 目录零文件（纪律遵守） | pass |
| 诚实性：5 条批准偏差、torch 未修公告、Compose 门禁 blocked 均与证据吻合，无夸大声明 | pass |

## 发现与处置

- **P2-1（合并前应修）**：规范文档未同步 403 边界错误码与清理队列语义 → 已由实现者补齐（三份规范同步提交），复核前置条件消除。
- **P3 ×7（记录即可）**：ACL `/grant:r` 不删除其他显式 ACE（继承已断，风险极低）；凭据 staging 的继承 ACL 短窗；凭据读取仅捕文件缺失（其他 OSError 以 500 呈现）；Origin 不拒绝 userinfo/伪端口形态（实证不可利用）；清理作业成功走 `_finish`（幂等可重跑）；上传主机校验为名字级（4a 已接受范围）；`testserver` 常驻测试 Host 允许列表（不可公共解析）。

## 结论

总裁决 **APPROVE-WITH-CONDITIONS**：未发现任何代码级安全回归或夸大实现；唯一实质发现为文档同步缺口（已处置）。复核明确建议批准合并——用户随后批准合并。本报告使该复核裁决进入正式治理记录。
