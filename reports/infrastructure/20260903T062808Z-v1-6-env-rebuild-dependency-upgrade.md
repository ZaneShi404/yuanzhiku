# v1-6-env-rebuild-dependency-upgrade：基础设施报告

- 报告 ID：`RPT-V1-6-ENV-REBUILD-DEPENDENCY-UPGRADE-20260903T062808Z-001`
- 记录时间（UTC）：`2026-09-03T06:28:08Z`
- 报告类型：`infrastructure`
- 作者角色：`infrastructure`
- 独立性：`non_independent`
- 产品版本：`v1.6.0`
- 裁定范围：`archive_local`
- 裁定：`accepted`

## 范围

本报告覆盖 v1.6.0 周期的执行环境与发布基础设施工作。关联需求：`REQ-002`、`REQ-045`、`REQ-046`。

1. **执行环境重建**：周期开始时发现本地虚拟环境已损坏（解释器缺失，任何测试命令无法执行）——按 `docs/dependency-installation.md` 从锁文件重建虚拟环境；重建暴露锁文件不可解冲突（docling 传递约束与 python-docx 锁定版本不兼容），按「仅当前 major 内最低修复」将 python-docx 升至 1.2.0 并完成整装验证。
2. **依赖审计与升级**（用户批准，审计数据为当日官方源）：npm 侧 nanoid、vite（连带 esbuild）升级后公告清零，前端三件套复验通过；Python 侧 starlette 0.49.1（4 条公告，fastapi 0.141.1 为解析器验证的联动版本）与 torch/torchaudio 2.10.0（2 条公告）升级后全量回归 489 通过、0 失败；pypdf 6.15.0 与 pytest 9.0.3 为用户批准的 major 跳升，升级后全量回归通过、测试套件零适配。torch 的 PYSEC-2025-194（修复版本 2.13.0）因 torchaudio 停更于 2.11.0 且严格配对而不可达，PYSEC-2026-139 上游无修复——均登记为已接受风险。
3. **发布基础设施**：仓库首次公开发布前的历史脱敏（三份跟踪文档的本地基础设施信息语义化改写 + `git filter-repo --replace-text` 全历史四字面量清除，全历史检索归零）；发布过程含一次替换规则转义事故，依赖重写前的完整镜像备份从仓库外备份文件完整恢复（树哈希逐一核对一致）后修正重跑；远端仓库推送经本机回环代理完成，代理配置以仓库级作用域固化。

## 验证

- 虚拟环境整装导入检查：fastapi、litellm、yt_dlp、funasr、docling、torch 全部导入成功，版本与锁文件一致。
- 依赖升级后全量回归 489 通过、0 失败（详见同周期测试报告）。
- 历史重写后核验：全部 83 个提交保留、当前树哈希与重写前逐字节一致（仅历史内容被清除）、新旧哈希映射表 84 条完整保存于 `tests/runtime/hardening-gateb-20260903/commit-map-old-to-new.txt`；四个敏感字面量的全历史检索计数归零。
- 远端仓库推送后引用核验：远端主分支指向与本地主分支一致。

## 结论

执行环境已从损坏状态重建为与锁文件逐版本一致的可用状态；依赖审计的可达修复全部落地并经全量回归验证；发布前历史脱敏完成且经全历史检索归零验证。镜像备份文件位于仓库外（不属于本档案），确认远端无误后由用户自行处置。
